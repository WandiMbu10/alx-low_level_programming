#include <stdio.h>

/**
 * add - function that adds two integers and return result
 * @a: parameter for int
 */
int add(int a, int b)
{
    /* add the two integers and return the result */
    return a + b;
}
