#include <stdio.h>

/**
 * add - checks that adds two integers and returns the results
 * @a: checks int to be added to a
 * @b: checks int to be added to b
 * Return: sum of a and b
 */

int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
