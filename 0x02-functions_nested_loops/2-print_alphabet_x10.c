#include "main.h"

/**
 * print_alphabet_x10 - a function that prints 10 times the alphabet
 *
 * Return: 0 (Success)
 */
void print_alphabet_x10(void)
{
	char ch;
	int i;
	int = 0;

	while (i = 1; i <= 10; i++)
	{
		for (ch = 'a'; ch <= 'z'; ch++)
		{
			_putchar(ch);
		}
	}
	_putchar('\n');
	i++;
}
int main(void)
{
	print_alphabet_x10();
	return (0);
}
